using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnPinkKoi : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        for (int i = 0; i < 20; i++)
        {
            //GameScarpManager.instance.pinkKois[0] = (float) UnityEngine.Random.Range(10, 40) * 0.1f;
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
